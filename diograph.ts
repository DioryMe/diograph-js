export default class Diograph {
  static hello = (str: string) => {
    console.log(str)
  }
  // constructor(height, width) {
  //   this.height = height
  //   this.width = width
  // }

  // load = () => {
  //   parsed_diograph = JSON.parse(File.read(path))
  //   diograph = parsed_diograph['diograph']
  //   root_id = parsed_diograph['rootId']
  // }
}
